{
  "private": true,
  "scripts": {
    "watch": "spago bundle-app --watch --source-maps --to static/halogen/index.js",
    "build": "spago bundle-app --to static/halogen/index.js"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "parcel": "^1.12.4",
    "purescript": "^0.13.8",
    "spago": "^0.19.0"
  }
}
